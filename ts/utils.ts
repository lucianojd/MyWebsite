export function loadEducationTable(content: string): void {
    var sesStrVar = "currEducationTable"

    // Used for organizing classes as they're read.
    class Class {
        name: string
        code: string
        URL: string
    }

    $.get()
}